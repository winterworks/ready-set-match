import { data } from "@/data/data";
import { Button, ButtonGroup } from "@mui/material";

export default function Home() {
  return (
    <main>
      <div>
        My app
      </div>
    </main>
  );
}
